export interface Match {
    uuid?: string;
    idUserAMatch: string;
    idUserBMatch: string;
    dateMatch: Date;
    showMatch: boolean;
    deletedMatch: boolean;
    createdAt: string;
    updatedAt: string;
}